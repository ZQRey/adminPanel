# CYBER//HUD Docker Compose Environment Configuration

# External Web Port on the Host (Default: 80, can be 8080, 8443, etc.)
PORT=80

# Secret Key for JWT Token Generation (Generate a random string for production)
SECRET_KEY=CYBER_HUD_SUPER_SECRET_KEY_9847291038472910

# 32-byte Fernet symmetric key for encrypting stored AD/Proxmox/NUT credentials
# To generate your own key, run: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=jZ4OqKvhQ86a3D2x_r1PZ4wK6y0f0U6A1p2B3C4D5E6=

# Token expiration in minutes (default: 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440
